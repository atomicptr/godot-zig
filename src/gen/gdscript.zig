// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// method bindings
var mbind_get_as_byte_code: ?*c_api.godot_method_bind = null;
var mbind_new: ?*c_api.godot_method_bind = null;
var mbind_gdscript_constructor: ?fn () ?*c_api.godot_object = null;

pub const GDScript = struct {
    const Self = @This();
    pub const BaseClass = godot.Script;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_gdscript_constructor == null) {
            mbind_gdscript_constructor = try api.createConstructor("GDScript");
        }
        return api.createObject(Self, mbind_gdscript_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getAsByteCode(self: *const Self) !godot.PoolByteArray {
        if (mbind_get_as_byte_code == null) {
            mbind_get_as_byte_code = try api.createMethod("GDScript", "get_as_byte_code");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_as_byte_code, base, cargs, result);
        return @ptrCast(*godot.PoolByteArray, @alignCast(@alignOf(&godot.PoolByteArray), result)).*;
    }

    pub fn new(self: *const Self) !godot.Variant {
        if (mbind_new == null) {
            mbind_new = try api.createMethod("GDScript", "new");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_new, base, cargs, result);
        return @ptrCast(*godot.Variant, @alignCast(@alignOf(&godot.Variant), result)).*;
    }
};
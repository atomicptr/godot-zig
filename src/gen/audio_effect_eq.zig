// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const AudioEffect = @import("audio_effect.zig").AudioEffect;

// method bindings
var mbind_get_band_count: ?*c_api.godot_method_bind = null;
var mbind_get_band_gain_db: ?*c_api.godot_method_bind = null;
var mbind_set_band_gain_db: ?*c_api.godot_method_bind = null;
var mbind_audio_effect_eq_constructor: ?fn () ?*c_api.godot_object = null;

pub const AudioEffectEQ = struct {
    const Self = @This();
    const BaseClass = AudioEffect;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_audio_effect_eq_constructor == null) {
            mbind_audio_effect_eq_constructor = try api.createConstructor("AudioEffectEQ");
        }
        return api.createObject(Self, mbind_audio_effect_eq_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getBandCount(self: *const Self) !i32 {
        if (mbind_get_band_count == null) {
            mbind_get_band_count = try api.createMethod("AudioEffectEQ", "get_band_count");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_band_count, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn getBandGainDb(self: *const Self, arg_band_idx: i32) !f32 {
        if (mbind_get_band_gain_db == null) {
            mbind_get_band_gain_db = try api.createMethod("AudioEffectEQ", "get_band_gain_db");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_band_idx),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_band_gain_db, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn setBandGainDb(self: *const Self, arg_band_idx: i32, arg_volume_db: f32) !void {
        if (mbind_set_band_gain_db == null) {
            mbind_set_band_gain_db = try api.createMethod("AudioEffectEQ", "set_band_gain_db");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_band_idx),
            @ptrCast(*const anyopaque, *arg_volume_db),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_band_gain_db, base, cargs, result);
    }
};
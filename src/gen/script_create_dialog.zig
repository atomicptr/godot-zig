// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// method bindings
var mbind_config: ?*c_api.godot_method_bind = null;
var mbind_script_create_dialog_constructor: ?fn () ?*c_api.godot_object = null;

pub const ScriptCreateDialog = struct {
    const Self = @This();
    pub const BaseClass = godot.ConfirmationDialog;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_script_create_dialog_constructor == null) {
            mbind_script_create_dialog_constructor = try api.createConstructor("ScriptCreateDialog");
        }
        return api.createObject(Self, mbind_script_create_dialog_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn config(self: *const Self, arg_inherits: *const godot.String, arg_path: *const godot.String, arg_built_in_enabled: bool, arg_load_enabled: bool) !void {
        if (mbind_config == null) {
            mbind_config = try api.createMethod("ScriptCreateDialog", "config");
        }

        var result: ?*anyopaque = null;
        var args: [4]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_inherits),
            @ptrCast(*const anyopaque, arg_path),
            @ptrCast(*const anyopaque, *arg_built_in_enabled),
            @ptrCast(*const anyopaque, *arg_load_enabled),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_config, base, cargs, result);
    }
};
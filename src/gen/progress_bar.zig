// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// method bindings
var mbind_is_percent_visible: ?*c_api.godot_method_bind = null;
var mbind_set_percent_visible: ?*c_api.godot_method_bind = null;
var mbind_progress_bar_constructor: ?fn () ?*c_api.godot_object = null;

pub const ProgressBar = struct {
    const Self = @This();
    pub const BaseClass = godot.Range;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_progress_bar_constructor == null) {
            mbind_progress_bar_constructor = try api.createConstructor("ProgressBar");
        }
        return api.createObject(Self, mbind_progress_bar_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn isPercentVisible(self: *const Self) !bool {
        if (mbind_is_percent_visible == null) {
            mbind_is_percent_visible = try api.createMethod("ProgressBar", "is_percent_visible");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_is_percent_visible, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn setPercentVisible(self: *const Self, arg_visible: bool) !void {
        if (mbind_set_percent_visible == null) {
            mbind_set_percent_visible = try api.createMethod("ProgressBar", "set_percent_visible");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_visible),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_percent_visible, base, cargs, result);
    }
};
// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const CSGShape = @import("csgshape.zig").CSGShape;

// method bindings
var mbind_is_inverting_faces: ?*c_api.godot_method_bind = null;
var mbind_set_invert_faces: ?*c_api.godot_method_bind = null;
var mbind_csgprimitive_constructor: ?fn () ?*c_api.godot_object = null;

pub const CSGPrimitive = struct {
    const Self = @This();
    const BaseClass = CSGShape;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_csgprimitive_constructor == null) {
            mbind_csgprimitive_constructor = try api.createConstructor("CSGPrimitive");
        }
        return api.createObject(Self, mbind_csgprimitive_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn isInvertingFaces(self: *const Self) !bool {
        if (mbind_is_inverting_faces == null) {
            mbind_is_inverting_faces = try api.createMethod("CSGPrimitive", "is_inverting_faces");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_is_inverting_faces, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn setInvertFaces(self: *const Self, arg_invert_faces: bool) !void {
        if (mbind_set_invert_faces == null) {
            mbind_set_invert_faces = try api.createMethod("CSGPrimitive", "set_invert_faces");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_invert_faces),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_invert_faces, base, cargs, result);
    }
};
// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const Script = @import("script.zig").Script;

// method bindings
var mbind_new: ?*c_api.godot_method_bind = null;
var mbind_plugin_script_constructor: ?fn () ?*c_api.godot_object = null;

pub const PluginScript = struct {
    const Self = @This();
    const BaseClass = Script;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_plugin_script_constructor == null) {
            mbind_plugin_script_constructor = try api.createConstructor("PluginScript");
        }
        return api.createObject(Self, mbind_plugin_script_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn new(self: *const Self) !godot.Variant {
        if (mbind_new == null) {
            mbind_new = try api.createMethod("PluginScript", "new");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_new, base, cargs, result);
        return @ptrCast(*godot.Variant, @alignCast(@alignOf(&godot.Variant), result)).*;
    }
};
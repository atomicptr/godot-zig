// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const Control = @import("control.zig").Control;

// method bindings
var mbind_get_frame_color: ?*c_api.godot_method_bind = null;
var mbind_set_frame_color: ?*c_api.godot_method_bind = null;
var mbind_color_rect_constructor: ?fn () ?*c_api.godot_object = null;

pub const ColorRect = struct {
    const Self = @This();
    const BaseClass = Control;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_color_rect_constructor == null) {
            mbind_color_rect_constructor = try api.createConstructor("ColorRect");
        }
        return api.createObject(Self, mbind_color_rect_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getFrameColor(self: *const Self) !godot.Color {
        if (mbind_get_frame_color == null) {
            mbind_get_frame_color = try api.createMethod("ColorRect", "get_frame_color");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_frame_color, base, cargs, result);
        return @ptrCast(*godot.Color, @alignCast(@alignOf(&godot.Color), result)).*;
    }

    pub fn setFrameColor(self: *const Self, arg_color: *const godot.Color) !void {
        if (mbind_set_frame_color == null) {
            mbind_set_frame_color = try api.createMethod("ColorRect", "set_frame_color");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_color),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_frame_color, base, cargs, result);
    }
};
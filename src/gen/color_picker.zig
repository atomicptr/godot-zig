// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// method bindings
var mbind_add_preset: ?*c_api.godot_method_bind = null;
var mbind_are_presets_enabled: ?*c_api.godot_method_bind = null;
var mbind_are_presets_visible: ?*c_api.godot_method_bind = null;
var mbind_erase_preset: ?*c_api.godot_method_bind = null;
var mbind_get_pick_color: ?*c_api.godot_method_bind = null;
var mbind_get_presets: ?*c_api.godot_method_bind = null;
var mbind_is_deferred_mode: ?*c_api.godot_method_bind = null;
var mbind_is_editing_alpha: ?*c_api.godot_method_bind = null;
var mbind_is_hsv_mode: ?*c_api.godot_method_bind = null;
var mbind_is_raw_mode: ?*c_api.godot_method_bind = null;
var mbind_set_deferred_mode: ?*c_api.godot_method_bind = null;
var mbind_set_edit_alpha: ?*c_api.godot_method_bind = null;
var mbind_set_hsv_mode: ?*c_api.godot_method_bind = null;
var mbind_set_pick_color: ?*c_api.godot_method_bind = null;
var mbind_set_presets_enabled: ?*c_api.godot_method_bind = null;
var mbind_set_presets_visible: ?*c_api.godot_method_bind = null;
var mbind_set_raw_mode: ?*c_api.godot_method_bind = null;
var mbind_color_picker_constructor: ?fn () ?*c_api.godot_object = null;

pub const ColorPicker = struct {
    const Self = @This();
    pub const BaseClass = godot.BoxContainer;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_color_picker_constructor == null) {
            mbind_color_picker_constructor = try api.createConstructor("ColorPicker");
        }
        return api.createObject(Self, mbind_color_picker_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn addPreset(self: *const Self, arg_color: *const godot.Color) !void {
        if (mbind_add_preset == null) {
            mbind_add_preset = try api.createMethod("ColorPicker", "add_preset");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_color),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_add_preset, base, cargs, result);
    }

    pub fn arePresetsEnabled(self: *const Self) !bool {
        if (mbind_are_presets_enabled == null) {
            mbind_are_presets_enabled = try api.createMethod("ColorPicker", "are_presets_enabled");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_are_presets_enabled, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn arePresetsVisible(self: *const Self) !bool {
        if (mbind_are_presets_visible == null) {
            mbind_are_presets_visible = try api.createMethod("ColorPicker", "are_presets_visible");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_are_presets_visible, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn erasePreset(self: *const Self, arg_color: *const godot.Color) !void {
        if (mbind_erase_preset == null) {
            mbind_erase_preset = try api.createMethod("ColorPicker", "erase_preset");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_color),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_erase_preset, base, cargs, result);
    }

    pub fn getPickColor(self: *const Self) !godot.Color {
        if (mbind_get_pick_color == null) {
            mbind_get_pick_color = try api.createMethod("ColorPicker", "get_pick_color");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_pick_color, base, cargs, result);
        return @ptrCast(*godot.Color, @alignCast(@alignOf(&godot.Color), result)).*;
    }

    pub fn getPresets(self: *const Self) !godot.PoolColorArray {
        if (mbind_get_presets == null) {
            mbind_get_presets = try api.createMethod("ColorPicker", "get_presets");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_presets, base, cargs, result);
        return @ptrCast(*godot.PoolColorArray, @alignCast(@alignOf(&godot.PoolColorArray), result)).*;
    }

    pub fn isDeferredMode(self: *const Self) !bool {
        if (mbind_is_deferred_mode == null) {
            mbind_is_deferred_mode = try api.createMethod("ColorPicker", "is_deferred_mode");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_is_deferred_mode, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn isEditingAlpha(self: *const Self) !bool {
        if (mbind_is_editing_alpha == null) {
            mbind_is_editing_alpha = try api.createMethod("ColorPicker", "is_editing_alpha");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_is_editing_alpha, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn isHsvMode(self: *const Self) !bool {
        if (mbind_is_hsv_mode == null) {
            mbind_is_hsv_mode = try api.createMethod("ColorPicker", "is_hsv_mode");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_is_hsv_mode, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn isRawMode(self: *const Self) !bool {
        if (mbind_is_raw_mode == null) {
            mbind_is_raw_mode = try api.createMethod("ColorPicker", "is_raw_mode");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_is_raw_mode, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn setDeferredMode(self: *const Self, arg_mode: bool) !void {
        if (mbind_set_deferred_mode == null) {
            mbind_set_deferred_mode = try api.createMethod("ColorPicker", "set_deferred_mode");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_mode),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_deferred_mode, base, cargs, result);
    }

    pub fn setEditAlpha(self: *const Self, arg_show: bool) !void {
        if (mbind_set_edit_alpha == null) {
            mbind_set_edit_alpha = try api.createMethod("ColorPicker", "set_edit_alpha");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_show),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_edit_alpha, base, cargs, result);
    }

    pub fn setHsvMode(self: *const Self, arg_mode: bool) !void {
        if (mbind_set_hsv_mode == null) {
            mbind_set_hsv_mode = try api.createMethod("ColorPicker", "set_hsv_mode");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_mode),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_hsv_mode, base, cargs, result);
    }

    pub fn setPickColor(self: *const Self, arg_color: *const godot.Color) !void {
        if (mbind_set_pick_color == null) {
            mbind_set_pick_color = try api.createMethod("ColorPicker", "set_pick_color");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_color),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_pick_color, base, cargs, result);
    }

    pub fn setPresetsEnabled(self: *const Self, arg_enabled: bool) !void {
        if (mbind_set_presets_enabled == null) {
            mbind_set_presets_enabled = try api.createMethod("ColorPicker", "set_presets_enabled");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_enabled),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_presets_enabled, base, cargs, result);
    }

    pub fn setPresetsVisible(self: *const Self, arg_visible: bool) !void {
        if (mbind_set_presets_visible == null) {
            mbind_set_presets_visible = try api.createMethod("ColorPicker", "set_presets_visible");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_visible),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_presets_visible, base, cargs, result);
    }

    pub fn setRawMode(self: *const Self, arg_mode: bool) !void {
        if (mbind_set_raw_mode == null) {
            mbind_set_raw_mode = try api.createMethod("ColorPicker", "set_raw_mode");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_mode),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_raw_mode, base, cargs, result);
    }
};
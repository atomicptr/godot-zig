// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// method bindings
var mbind_get_buffer_length: ?*c_api.godot_method_bind = null;
var mbind_get_fft_size: ?*c_api.godot_method_bind = null;
var mbind_get_tap_back_pos: ?*c_api.godot_method_bind = null;
var mbind_set_buffer_length: ?*c_api.godot_method_bind = null;
var mbind_set_fft_size: ?*c_api.godot_method_bind = null;
var mbind_set_tap_back_pos: ?*c_api.godot_method_bind = null;
var mbind_audio_effect_spectrum_analyzer_constructor: ?fn () ?*c_api.godot_object = null;

pub const AudioEffectSpectrumAnalyzer = struct {
    const Self = @This();
    pub const BaseClass = godot.AudioEffect;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_audio_effect_spectrum_analyzer_constructor == null) {
            mbind_audio_effect_spectrum_analyzer_constructor = try api.createConstructor("AudioEffectSpectrumAnalyzer");
        }
        return api.createObject(Self, mbind_audio_effect_spectrum_analyzer_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getBufferLength(self: *const Self) !f32 {
        if (mbind_get_buffer_length == null) {
            mbind_get_buffer_length = try api.createMethod("AudioEffectSpectrumAnalyzer", "get_buffer_length");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_buffer_length, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getFftSize(self: *const Self) !i32 {
        if (mbind_get_fft_size == null) {
            mbind_get_fft_size = try api.createMethod("AudioEffectSpectrumAnalyzer", "get_fft_size");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_fft_size, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn getTapBackPos(self: *const Self) !f32 {
        if (mbind_get_tap_back_pos == null) {
            mbind_get_tap_back_pos = try api.createMethod("AudioEffectSpectrumAnalyzer", "get_tap_back_pos");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_tap_back_pos, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn setBufferLength(self: *const Self, arg_seconds: f32) !void {
        if (mbind_set_buffer_length == null) {
            mbind_set_buffer_length = try api.createMethod("AudioEffectSpectrumAnalyzer", "set_buffer_length");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_seconds),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_buffer_length, base, cargs, result);
    }

    pub fn setFftSize(self: *const Self, arg_size: i32) !void {
        if (mbind_set_fft_size == null) {
            mbind_set_fft_size = try api.createMethod("AudioEffectSpectrumAnalyzer", "set_fft_size");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_size),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_fft_size, base, cargs, result);
    }

    pub fn setTapBackPos(self: *const Self, arg_seconds: f32) !void {
        if (mbind_set_tap_back_pos == null) {
            mbind_set_tap_back_pos = try api.createMethod("AudioEffectSpectrumAnalyzer", "set_tap_back_pos");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_seconds),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_tap_back_pos, base, cargs, result);
    }
};
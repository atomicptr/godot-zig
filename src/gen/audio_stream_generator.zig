// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const AudioStream = @import("audio_stream.zig").AudioStream;

// method bindings
var mbind_get_buffer_length: ?*c_api.godot_method_bind = null;
var mbind_get_mix_rate: ?*c_api.godot_method_bind = null;
var mbind_set_buffer_length: ?*c_api.godot_method_bind = null;
var mbind_set_mix_rate: ?*c_api.godot_method_bind = null;
var mbind_audio_stream_generator_constructor: ?fn () ?*c_api.godot_object = null;

pub const AudioStreamGenerator = struct {
    const Self = @This();
    const BaseClass = AudioStream;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_audio_stream_generator_constructor == null) {
            mbind_audio_stream_generator_constructor = try api.createConstructor("AudioStreamGenerator");
        }
        return api.createObject(Self, mbind_audio_stream_generator_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getBufferLength(self: *const Self) !f32 {
        if (mbind_get_buffer_length == null) {
            mbind_get_buffer_length = try api.createMethod("AudioStreamGenerator", "get_buffer_length");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_buffer_length, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getMixRate(self: *const Self) !f32 {
        if (mbind_get_mix_rate == null) {
            mbind_get_mix_rate = try api.createMethod("AudioStreamGenerator", "get_mix_rate");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_mix_rate, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn setBufferLength(self: *const Self, arg_seconds: f32) !void {
        if (mbind_set_buffer_length == null) {
            mbind_set_buffer_length = try api.createMethod("AudioStreamGenerator", "set_buffer_length");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_seconds),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_buffer_length, base, cargs, result);
    }

    pub fn setMixRate(self: *const Self, arg_hz: f32) !void {
        if (mbind_set_mix_rate == null) {
            mbind_set_mix_rate = try api.createMethod("AudioStreamGenerator", "set_mix_rate");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_hz),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_mix_rate, base, cargs, result);
    }
};
// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const AudioEffect = @import("audio_effect.zig").AudioEffect;

// method bindings
var mbind_get_pan_pullout: ?*c_api.godot_method_bind = null;
var mbind_get_surround: ?*c_api.godot_method_bind = null;
var mbind_get_time_pullout: ?*c_api.godot_method_bind = null;
var mbind_set_pan_pullout: ?*c_api.godot_method_bind = null;
var mbind_set_surround: ?*c_api.godot_method_bind = null;
var mbind_set_time_pullout: ?*c_api.godot_method_bind = null;
var mbind_audio_effect_stereo_enhance_constructor: ?fn () ?*c_api.godot_object = null;

pub const AudioEffectStereoEnhance = struct {
    const Self = @This();
    const BaseClass = AudioEffect;

    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_audio_effect_stereo_enhance_constructor == null) {
            mbind_audio_effect_stereo_enhance_constructor = try api.createConstructor("AudioEffectStereoEnhance");
        }
        return api.createObject(Self, mbind_audio_effect_stereo_enhance_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getPanPullout(self: *const Self) !f32 {
        if (mbind_get_pan_pullout == null) {
            mbind_get_pan_pullout = try api.createMethod("AudioEffectStereoEnhance", "get_pan_pullout");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_pan_pullout, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getSurround(self: *const Self) !f32 {
        if (mbind_get_surround == null) {
            mbind_get_surround = try api.createMethod("AudioEffectStereoEnhance", "get_surround");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_surround, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getTimePullout(self: *const Self) !f32 {
        if (mbind_get_time_pullout == null) {
            mbind_get_time_pullout = try api.createMethod("AudioEffectStereoEnhance", "get_time_pullout");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_time_pullout, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn setPanPullout(self: *const Self, arg_amount: f32) !void {
        if (mbind_set_pan_pullout == null) {
            mbind_set_pan_pullout = try api.createMethod("AudioEffectStereoEnhance", "set_pan_pullout");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_amount),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_pan_pullout, base, cargs, result);
    }

    pub fn setSurround(self: *const Self, arg_amount: f32) !void {
        if (mbind_set_surround == null) {
            mbind_set_surround = try api.createMethod("AudioEffectStereoEnhance", "set_surround");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_amount),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_surround, base, cargs, result);
    }

    pub fn setTimePullout(self: *const Self, arg_amount: f32) !void {
        if (mbind_set_time_pullout == null) {
            mbind_set_time_pullout = try api.createMethod("AudioEffectStereoEnhance", "set_time_pullout");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_amount),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_time_pullout, base, cargs, result);
    }
};
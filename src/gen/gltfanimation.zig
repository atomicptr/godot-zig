// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const Resource = @import("resource.zig").Resource;

// method bindings
var mbind_get_loop: ?*c_api.godot_method_bind = null;
var mbind_set_loop: ?*c_api.godot_method_bind = null;
var mbind_gltfanimation_constructor: ?fn () ?*c_api.godot_object = null;

pub const GLTFAnimation = struct {
    const Self = @This();
    const BaseClass = Resource;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_gltfanimation_constructor == null) {
            mbind_gltfanimation_constructor = try api.createConstructor("GLTFAnimation");
        }
        return api.createObject(Self, mbind_gltfanimation_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getLoop(self: *const Self) !bool {
        if (mbind_get_loop == null) {
            mbind_get_loop = try api.createMethod("GLTFAnimation", "get_loop");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_loop, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn setLoop(self: *const Self, arg_loop: bool) !void {
        if (mbind_set_loop == null) {
            mbind_set_loop = try api.createMethod("GLTFAnimation", "set_loop");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_loop),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_loop, base, cargs, result);
    }
};
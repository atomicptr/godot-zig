// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const StyleBox = @import("style_box.zig").StyleBox;

// method bindings
var mbind_get_expand_margin_size: ?*c_api.godot_method_bind = null;
var mbind_get_h_axis_stretch_mode: ?*c_api.godot_method_bind = null;
var mbind_get_margin_size: ?*c_api.godot_method_bind = null;
var mbind_get_modulate: ?*c_api.godot_method_bind = null;
var mbind_get_normal_map: ?*c_api.godot_method_bind = null;
var mbind_get_region_rect: ?*c_api.godot_method_bind = null;
var mbind_get_texture: ?*c_api.godot_method_bind = null;
var mbind_get_v_axis_stretch_mode: ?*c_api.godot_method_bind = null;
var mbind_is_draw_center_enabled: ?*c_api.godot_method_bind = null;
var mbind_set_draw_center: ?*c_api.godot_method_bind = null;
var mbind_set_expand_margin_all: ?*c_api.godot_method_bind = null;
var mbind_set_expand_margin_individual: ?*c_api.godot_method_bind = null;
var mbind_set_expand_margin_size: ?*c_api.godot_method_bind = null;
var mbind_set_h_axis_stretch_mode: ?*c_api.godot_method_bind = null;
var mbind_set_margin_size: ?*c_api.godot_method_bind = null;
var mbind_set_modulate: ?*c_api.godot_method_bind = null;
var mbind_set_normal_map: ?*c_api.godot_method_bind = null;
var mbind_set_region_rect: ?*c_api.godot_method_bind = null;
var mbind_set_texture: ?*c_api.godot_method_bind = null;
var mbind_set_v_axis_stretch_mode: ?*c_api.godot_method_bind = null;
var mbind_style_box_texture_constructor: ?fn () ?*c_api.godot_object = null;

pub const StyleBoxTexture = struct {
    const Self = @This();
    const BaseClass = StyleBox;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_style_box_texture_constructor == null) {
            mbind_style_box_texture_constructor = try api.createConstructor("StyleBoxTexture");
        }
        return api.createObject(Self, mbind_style_box_texture_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getExpandMarginSize(self: *const Self, arg_margin: i32) !f32 {
        if (mbind_get_expand_margin_size == null) {
            mbind_get_expand_margin_size = try api.createMethod("StyleBoxTexture", "get_expand_margin_size");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_margin),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_expand_margin_size, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getHAxisStretchMode(self: *const Self) !i32 {
        if (mbind_get_h_axis_stretch_mode == null) {
            mbind_get_h_axis_stretch_mode = try api.createMethod("StyleBoxTexture", "get_h_axis_stretch_mode");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_h_axis_stretch_mode, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn getMarginSize(self: *const Self, arg_margin: i32) !f32 {
        if (mbind_get_margin_size == null) {
            mbind_get_margin_size = try api.createMethod("StyleBoxTexture", "get_margin_size");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_margin),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_margin_size, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getModulate(self: *const Self) !godot.Color {
        if (mbind_get_modulate == null) {
            mbind_get_modulate = try api.createMethod("StyleBoxTexture", "get_modulate");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_modulate, base, cargs, result);
        return @ptrCast(*godot.Color, @alignCast(@alignOf(&godot.Color), result)).*;
    }

    pub fn getNormalMap(self: *const Self) !godot.Texture {
        if (mbind_get_normal_map == null) {
            mbind_get_normal_map = try api.createMethod("StyleBoxTexture", "get_normal_map");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_normal_map, base, cargs, result);
        return @ptrCast(*godot.Texture, @alignCast(@alignOf(&godot.Texture), result)).*;
    }

    pub fn getRegionRect(self: *const Self) !godot.Rect2 {
        if (mbind_get_region_rect == null) {
            mbind_get_region_rect = try api.createMethod("StyleBoxTexture", "get_region_rect");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_region_rect, base, cargs, result);
        return @ptrCast(*godot.Rect2, @alignCast(@alignOf(&godot.Rect2), result)).*;
    }

    pub fn getTexture(self: *const Self) !godot.Texture {
        if (mbind_get_texture == null) {
            mbind_get_texture = try api.createMethod("StyleBoxTexture", "get_texture");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_texture, base, cargs, result);
        return @ptrCast(*godot.Texture, @alignCast(@alignOf(&godot.Texture), result)).*;
    }

    pub fn getVAxisStretchMode(self: *const Self) !i32 {
        if (mbind_get_v_axis_stretch_mode == null) {
            mbind_get_v_axis_stretch_mode = try api.createMethod("StyleBoxTexture", "get_v_axis_stretch_mode");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_v_axis_stretch_mode, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn isDrawCenterEnabled(self: *const Self) !bool {
        if (mbind_is_draw_center_enabled == null) {
            mbind_is_draw_center_enabled = try api.createMethod("StyleBoxTexture", "is_draw_center_enabled");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_is_draw_center_enabled, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn setDrawCenter(self: *const Self, arg_enable: bool) !void {
        if (mbind_set_draw_center == null) {
            mbind_set_draw_center = try api.createMethod("StyleBoxTexture", "set_draw_center");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_enable),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_draw_center, base, cargs, result);
    }

    pub fn setExpandMarginAll(self: *const Self, arg_size: f32) !void {
        if (mbind_set_expand_margin_all == null) {
            mbind_set_expand_margin_all = try api.createMethod("StyleBoxTexture", "set_expand_margin_all");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_size),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_expand_margin_all, base, cargs, result);
    }

    pub fn setExpandMarginIndividual(self: *const Self, arg_size_left: f32, arg_size_top: f32, arg_size_right: f32, arg_size_bottom: f32) !void {
        if (mbind_set_expand_margin_individual == null) {
            mbind_set_expand_margin_individual = try api.createMethod("StyleBoxTexture", "set_expand_margin_individual");
        }

        var result: ?*anyopaque = null;
        var args: [4]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_size_left),
            @ptrCast(*const anyopaque, *arg_size_top),
            @ptrCast(*const anyopaque, *arg_size_right),
            @ptrCast(*const anyopaque, *arg_size_bottom),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_expand_margin_individual, base, cargs, result);
    }

    pub fn setExpandMarginSize(self: *const Self, arg_margin: i32, arg_size: f32) !void {
        if (mbind_set_expand_margin_size == null) {
            mbind_set_expand_margin_size = try api.createMethod("StyleBoxTexture", "set_expand_margin_size");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_margin),
            @ptrCast(*const anyopaque, *arg_size),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_expand_margin_size, base, cargs, result);
    }

    pub fn setHAxisStretchMode(self: *const Self, arg_mode: i32) !void {
        if (mbind_set_h_axis_stretch_mode == null) {
            mbind_set_h_axis_stretch_mode = try api.createMethod("StyleBoxTexture", "set_h_axis_stretch_mode");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_mode),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_h_axis_stretch_mode, base, cargs, result);
    }

    pub fn setMarginSize(self: *const Self, arg_margin: i32, arg_size: f32) !void {
        if (mbind_set_margin_size == null) {
            mbind_set_margin_size = try api.createMethod("StyleBoxTexture", "set_margin_size");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_margin),
            @ptrCast(*const anyopaque, *arg_size),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_margin_size, base, cargs, result);
    }

    pub fn setModulate(self: *const Self, arg_color: *const godot.Color) !void {
        if (mbind_set_modulate == null) {
            mbind_set_modulate = try api.createMethod("StyleBoxTexture", "set_modulate");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_color),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_modulate, base, cargs, result);
    }

    pub fn setNormalMap(self: *const Self, arg_normal_map: *const godot.Texture) !void {
        if (mbind_set_normal_map == null) {
            mbind_set_normal_map = try api.createMethod("StyleBoxTexture", "set_normal_map");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_normal_map),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_normal_map, base, cargs, result);
    }

    pub fn setRegionRect(self: *const Self, arg_region: *const godot.Rect2) !void {
        if (mbind_set_region_rect == null) {
            mbind_set_region_rect = try api.createMethod("StyleBoxTexture", "set_region_rect");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_region),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_region_rect, base, cargs, result);
    }

    pub fn setTexture(self: *const Self, arg_texture: *const godot.Texture) !void {
        if (mbind_set_texture == null) {
            mbind_set_texture = try api.createMethod("StyleBoxTexture", "set_texture");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_texture),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_texture, base, cargs, result);
    }

    pub fn setVAxisStretchMode(self: *const Self, arg_mode: i32) !void {
        if (mbind_set_v_axis_stretch_mode == null) {
            mbind_set_v_axis_stretch_mode = try api.createMethod("StyleBoxTexture", "set_v_axis_stretch_mode");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_mode),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_v_axis_stretch_mode, base, cargs, result);
    }
};
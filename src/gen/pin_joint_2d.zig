// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const Joint2D = @import("joint_2d.zig").Joint2D;

// method bindings
var mbind_get_softness: ?*c_api.godot_method_bind = null;
var mbind_set_softness: ?*c_api.godot_method_bind = null;
var mbind_pin_joint_2d_constructor: ?fn () ?*c_api.godot_object = null;

pub const PinJoint2D = struct {
    const Self = @This();
    const BaseClass = Joint2D;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_pin_joint_2d_constructor == null) {
            mbind_pin_joint_2d_constructor = try api.createConstructor("PinJoint2D");
        }
        return api.createObject(Self, mbind_pin_joint_2d_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getSoftness(self: *const Self) !f32 {
        if (mbind_get_softness == null) {
            mbind_get_softness = try api.createMethod("PinJoint2D", "get_softness");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_softness, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn setSoftness(self: *const Self, arg_softness: f32) !void {
        if (mbind_set_softness == null) {
            mbind_set_softness = try api.createMethod("PinJoint2D", "set_softness");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_softness),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_softness, base, cargs, result);
    }
};
// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const Joint = @import("joint.zig").Joint;

// method bindings
var mbind_get_flag_x: ?*c_api.godot_method_bind = null;
var mbind_get_flag_y: ?*c_api.godot_method_bind = null;
var mbind_get_flag_z: ?*c_api.godot_method_bind = null;
var mbind_get_param_x: ?*c_api.godot_method_bind = null;
var mbind_get_param_y: ?*c_api.godot_method_bind = null;
var mbind_get_param_z: ?*c_api.godot_method_bind = null;
var mbind_set_flag_x: ?*c_api.godot_method_bind = null;
var mbind_set_flag_y: ?*c_api.godot_method_bind = null;
var mbind_set_flag_z: ?*c_api.godot_method_bind = null;
var mbind_set_param_x: ?*c_api.godot_method_bind = null;
var mbind_set_param_y: ?*c_api.godot_method_bind = null;
var mbind_set_param_z: ?*c_api.godot_method_bind = null;
var mbind_generic_6dofjoint_constructor: ?fn () ?*c_api.godot_object = null;

pub const Generic6DOFJoint = struct {
    const Self = @This();
    const BaseClass = Joint;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_generic_6dofjoint_constructor == null) {
            mbind_generic_6dofjoint_constructor = try api.createConstructor("Generic6DOFJoint");
        }
        return api.createObject(Self, mbind_generic_6dofjoint_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getFlagX(self: *const Self, arg_flag: i32) !bool {
        if (mbind_get_flag_x == null) {
            mbind_get_flag_x = try api.createMethod("Generic6DOFJoint", "get_flag_x");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_flag),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_flag_x, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn getFlagY(self: *const Self, arg_flag: i32) !bool {
        if (mbind_get_flag_y == null) {
            mbind_get_flag_y = try api.createMethod("Generic6DOFJoint", "get_flag_y");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_flag),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_flag_y, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn getFlagZ(self: *const Self, arg_flag: i32) !bool {
        if (mbind_get_flag_z == null) {
            mbind_get_flag_z = try api.createMethod("Generic6DOFJoint", "get_flag_z");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_flag),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_flag_z, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn getParamX(self: *const Self, arg_param: i32) !f32 {
        if (mbind_get_param_x == null) {
            mbind_get_param_x = try api.createMethod("Generic6DOFJoint", "get_param_x");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_param),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_param_x, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getParamY(self: *const Self, arg_param: i32) !f32 {
        if (mbind_get_param_y == null) {
            mbind_get_param_y = try api.createMethod("Generic6DOFJoint", "get_param_y");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_param),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_param_y, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getParamZ(self: *const Self, arg_param: i32) !f32 {
        if (mbind_get_param_z == null) {
            mbind_get_param_z = try api.createMethod("Generic6DOFJoint", "get_param_z");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_param),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_param_z, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn setFlagX(self: *const Self, arg_flag: i32, arg_value: bool) !void {
        if (mbind_set_flag_x == null) {
            mbind_set_flag_x = try api.createMethod("Generic6DOFJoint", "set_flag_x");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_flag),
            @ptrCast(*const anyopaque, *arg_value),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_flag_x, base, cargs, result);
    }

    pub fn setFlagY(self: *const Self, arg_flag: i32, arg_value: bool) !void {
        if (mbind_set_flag_y == null) {
            mbind_set_flag_y = try api.createMethod("Generic6DOFJoint", "set_flag_y");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_flag),
            @ptrCast(*const anyopaque, *arg_value),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_flag_y, base, cargs, result);
    }

    pub fn setFlagZ(self: *const Self, arg_flag: i32, arg_value: bool) !void {
        if (mbind_set_flag_z == null) {
            mbind_set_flag_z = try api.createMethod("Generic6DOFJoint", "set_flag_z");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_flag),
            @ptrCast(*const anyopaque, *arg_value),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_flag_z, base, cargs, result);
    }

    pub fn setParamX(self: *const Self, arg_param: i32, arg_value: f32) !void {
        if (mbind_set_param_x == null) {
            mbind_set_param_x = try api.createMethod("Generic6DOFJoint", "set_param_x");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_param),
            @ptrCast(*const anyopaque, *arg_value),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_param_x, base, cargs, result);
    }

    pub fn setParamY(self: *const Self, arg_param: i32, arg_value: f32) !void {
        if (mbind_set_param_y == null) {
            mbind_set_param_y = try api.createMethod("Generic6DOFJoint", "set_param_y");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_param),
            @ptrCast(*const anyopaque, *arg_value),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_param_y, base, cargs, result);
    }

    pub fn setParamZ(self: *const Self, arg_param: i32, arg_value: f32) !void {
        if (mbind_set_param_z == null) {
            mbind_set_param_z = try api.createMethod("Generic6DOFJoint", "set_param_z");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_param),
            @ptrCast(*const anyopaque, *arg_value),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_param_z, base, cargs, result);
    }
};
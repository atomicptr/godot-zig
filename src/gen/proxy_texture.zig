// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// method bindings
var mbind_get_base: ?*c_api.godot_method_bind = null;
var mbind_set_base: ?*c_api.godot_method_bind = null;
var mbind_proxy_texture_constructor: ?fn () ?*c_api.godot_object = null;

pub const ProxyTexture = struct {
    const Self = @This();
    pub const BaseClass = godot.Texture;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_proxy_texture_constructor == null) {
            mbind_proxy_texture_constructor = try api.createConstructor("ProxyTexture");
        }
        return api.createObject(Self, mbind_proxy_texture_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getBase(self: *const Self) !godot.Texture {
        if (mbind_get_base == null) {
            mbind_get_base = try api.createMethod("ProxyTexture", "get_base");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_base, base, cargs, result);
        return @ptrCast(*godot.Texture, @alignCast(@alignOf(&godot.Texture), result)).*;
    }

    pub fn setBase(self: *const Self, arg_base: *const godot.Texture) !void {
        if (mbind_set_base == null) {
            mbind_set_base = try api.createMethod("ProxyTexture", "set_base");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_base),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_base, base, cargs, result);
    }
};
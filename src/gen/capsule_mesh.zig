// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const PrimitiveMesh = @import("primitive_mesh.zig").PrimitiveMesh;

// method bindings
var mbind_get_mid_height: ?*c_api.godot_method_bind = null;
var mbind_get_radial_segments: ?*c_api.godot_method_bind = null;
var mbind_get_radius: ?*c_api.godot_method_bind = null;
var mbind_get_rings: ?*c_api.godot_method_bind = null;
var mbind_set_mid_height: ?*c_api.godot_method_bind = null;
var mbind_set_radial_segments: ?*c_api.godot_method_bind = null;
var mbind_set_radius: ?*c_api.godot_method_bind = null;
var mbind_set_rings: ?*c_api.godot_method_bind = null;
var mbind_capsule_mesh_constructor: ?fn () ?*c_api.godot_object = null;

pub const CapsuleMesh = struct {
    const Self = @This();
    const BaseClass = PrimitiveMesh;

    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_capsule_mesh_constructor == null) {
            mbind_capsule_mesh_constructor = try api.createConstructor("CapsuleMesh");
        }
        return api.createObject(Self, mbind_capsule_mesh_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getMidHeight(self: *const Self) !f32 {
        if (mbind_get_mid_height == null) {
            mbind_get_mid_height = try api.createMethod("CapsuleMesh", "get_mid_height");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_mid_height, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getRadialSegments(self: *const Self) !i32 {
        if (mbind_get_radial_segments == null) {
            mbind_get_radial_segments = try api.createMethod("CapsuleMesh", "get_radial_segments");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_radial_segments, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn getRadius(self: *const Self) !f32 {
        if (mbind_get_radius == null) {
            mbind_get_radius = try api.createMethod("CapsuleMesh", "get_radius");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_radius, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getRings(self: *const Self) !i32 {
        if (mbind_get_rings == null) {
            mbind_get_rings = try api.createMethod("CapsuleMesh", "get_rings");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_rings, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn setMidHeight(self: *const Self, arg_mid_height: f32) !void {
        if (mbind_set_mid_height == null) {
            mbind_set_mid_height = try api.createMethod("CapsuleMesh", "set_mid_height");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_mid_height),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_mid_height, base, cargs, result);
    }

    pub fn setRadialSegments(self: *const Self, arg_segments: i32) !void {
        if (mbind_set_radial_segments == null) {
            mbind_set_radial_segments = try api.createMethod("CapsuleMesh", "set_radial_segments");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_segments),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_radial_segments, base, cargs, result);
    }

    pub fn setRadius(self: *const Self, arg_radius: f32) !void {
        if (mbind_set_radius == null) {
            mbind_set_radius = try api.createMethod("CapsuleMesh", "set_radius");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_radius),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_radius, base, cargs, result);
    }

    pub fn setRings(self: *const Self, arg_rings: i32) !void {
        if (mbind_set_rings == null) {
            mbind_set_rings = try api.createMethod("CapsuleMesh", "set_rings");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_rings),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_rings, base, cargs, result);
    }
};
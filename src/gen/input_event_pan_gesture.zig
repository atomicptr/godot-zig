// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const InputEventGesture = @import("input_event_gesture.zig").InputEventGesture;

// method bindings
var mbind_get_delta: ?*c_api.godot_method_bind = null;
var mbind_set_delta: ?*c_api.godot_method_bind = null;
var mbind_input_event_pan_gesture_constructor: ?fn () ?*c_api.godot_object = null;

pub const InputEventPanGesture = struct {
    const Self = @This();
    const BaseClass = InputEventGesture;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_input_event_pan_gesture_constructor == null) {
            mbind_input_event_pan_gesture_constructor = try api.createConstructor("InputEventPanGesture");
        }
        return api.createObject(Self, mbind_input_event_pan_gesture_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getDelta(self: *const Self) !godot.Vector2 {
        if (mbind_get_delta == null) {
            mbind_get_delta = try api.createMethod("InputEventPanGesture", "get_delta");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_delta, base, cargs, result);
        return @ptrCast(*godot.Vector2, @alignCast(@alignOf(&godot.Vector2), result)).*;
    }

    pub fn setDelta(self: *const Self, arg_delta: *const godot.Vector2) !void {
        if (mbind_set_delta == null) {
            mbind_set_delta = try api.createMethod("InputEventPanGesture", "set_delta");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_delta),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_delta, base, cargs, result);
    }
};
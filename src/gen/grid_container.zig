// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const Container = @import("container.zig").Container;

// method bindings
var mbind_get_columns: ?*c_api.godot_method_bind = null;
var mbind_set_columns: ?*c_api.godot_method_bind = null;
var mbind_grid_container_constructor: ?fn () ?*c_api.godot_object = null;

pub const GridContainer = struct {
    const Self = @This();
    const BaseClass = Container;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_grid_container_constructor == null) {
            mbind_grid_container_constructor = try api.createConstructor("GridContainer");
        }
        return api.createObject(Self, mbind_grid_container_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getColumns(self: *const Self) !i32 {
        if (mbind_get_columns == null) {
            mbind_get_columns = try api.createMethod("GridContainer", "get_columns");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_columns, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn setColumns(self: *const Self, arg_columns: i32) !void {
        if (mbind_set_columns == null) {
            mbind_set_columns = try api.createMethod("GridContainer", "set_columns");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_columns),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_columns, base, cargs, result);
    }
};
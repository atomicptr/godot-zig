// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const AudioEffectInstance = @import("audio_effect_instance.zig").AudioEffectInstance;

// method bindings
var mbind_get_magnitude_for_frequency_range: ?*c_api.godot_method_bind = null;
var mbind_audio_effect_spectrum_analyzer_instance_constructor: ?fn () ?*c_api.godot_object = null;

pub const AudioEffectSpectrumAnalyzerInstance = struct {
    const Self = @This();
    const BaseClass = AudioEffectInstance;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_audio_effect_spectrum_analyzer_instance_constructor == null) {
            mbind_audio_effect_spectrum_analyzer_instance_constructor = try api.createConstructor("AudioEffectSpectrumAnalyzerInstance");
        }
        return api.createObject(Self, mbind_audio_effect_spectrum_analyzer_instance_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getMagnitudeForFrequencyRange(self: *const Self, arg_from_hz: f32, arg_to_hz: f32, arg_mode: i32) !godot.Vector2 {
        if (mbind_get_magnitude_for_frequency_range == null) {
            mbind_get_magnitude_for_frequency_range = try api.createMethod("AudioEffectSpectrumAnalyzerInstance", "get_magnitude_for_frequency_range");
        }

        var result: ?*anyopaque = null;
        var args: [3]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_from_hz),
            @ptrCast(*const anyopaque, *arg_to_hz),
            @ptrCast(*const anyopaque, *arg_mode),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_magnitude_for_frequency_range, base, cargs, result);
        return @ptrCast(*godot.Vector2, @alignCast(@alignOf(&godot.Vector2), result)).*;
    }
};
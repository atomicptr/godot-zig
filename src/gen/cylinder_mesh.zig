// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const PrimitiveMesh = @import("primitive_mesh.zig").PrimitiveMesh;

// method bindings
var mbind_get_bottom_radius: ?*c_api.godot_method_bind = null;
var mbind_get_height: ?*c_api.godot_method_bind = null;
var mbind_get_radial_segments: ?*c_api.godot_method_bind = null;
var mbind_get_rings: ?*c_api.godot_method_bind = null;
var mbind_get_top_radius: ?*c_api.godot_method_bind = null;
var mbind_set_bottom_radius: ?*c_api.godot_method_bind = null;
var mbind_set_height: ?*c_api.godot_method_bind = null;
var mbind_set_radial_segments: ?*c_api.godot_method_bind = null;
var mbind_set_rings: ?*c_api.godot_method_bind = null;
var mbind_set_top_radius: ?*c_api.godot_method_bind = null;
var mbind_cylinder_mesh_constructor: ?fn () ?*c_api.godot_object = null;

pub const CylinderMesh = struct {
    const Self = @This();
    const BaseClass = PrimitiveMesh;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_cylinder_mesh_constructor == null) {
            mbind_cylinder_mesh_constructor = try api.createConstructor("CylinderMesh");
        }
        return api.createObject(Self, mbind_cylinder_mesh_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getBottomRadius(self: *const Self) !f32 {
        if (mbind_get_bottom_radius == null) {
            mbind_get_bottom_radius = try api.createMethod("CylinderMesh", "get_bottom_radius");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_bottom_radius, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getHeight(self: *const Self) !f32 {
        if (mbind_get_height == null) {
            mbind_get_height = try api.createMethod("CylinderMesh", "get_height");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_height, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getRadialSegments(self: *const Self) !i32 {
        if (mbind_get_radial_segments == null) {
            mbind_get_radial_segments = try api.createMethod("CylinderMesh", "get_radial_segments");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_radial_segments, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn getRings(self: *const Self) !i32 {
        if (mbind_get_rings == null) {
            mbind_get_rings = try api.createMethod("CylinderMesh", "get_rings");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_rings, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn getTopRadius(self: *const Self) !f32 {
        if (mbind_get_top_radius == null) {
            mbind_get_top_radius = try api.createMethod("CylinderMesh", "get_top_radius");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_top_radius, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn setBottomRadius(self: *const Self, arg_radius: f32) !void {
        if (mbind_set_bottom_radius == null) {
            mbind_set_bottom_radius = try api.createMethod("CylinderMesh", "set_bottom_radius");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_radius),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_bottom_radius, base, cargs, result);
    }

    pub fn setHeight(self: *const Self, arg_height: f32) !void {
        if (mbind_set_height == null) {
            mbind_set_height = try api.createMethod("CylinderMesh", "set_height");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_height),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_height, base, cargs, result);
    }

    pub fn setRadialSegments(self: *const Self, arg_segments: i32) !void {
        if (mbind_set_radial_segments == null) {
            mbind_set_radial_segments = try api.createMethod("CylinderMesh", "set_radial_segments");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_segments),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_radial_segments, base, cargs, result);
    }

    pub fn setRings(self: *const Self, arg_rings: i32) !void {
        if (mbind_set_rings == null) {
            mbind_set_rings = try api.createMethod("CylinderMesh", "set_rings");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_rings),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_rings, base, cargs, result);
    }

    pub fn setTopRadius(self: *const Self, arg_radius: f32) !void {
        if (mbind_set_top_radius == null) {
            mbind_set_top_radius = try api.createMethod("CylinderMesh", "set_top_radius");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_radius),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_top_radius, base, cargs, result);
    }
};
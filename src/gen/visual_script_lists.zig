// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const VisualScriptNode = @import("visual_script_node.zig").VisualScriptNode;

// method bindings
var mbind_add_input_data_port: ?*c_api.godot_method_bind = null;
var mbind_add_output_data_port: ?*c_api.godot_method_bind = null;
var mbind_remove_input_data_port: ?*c_api.godot_method_bind = null;
var mbind_remove_output_data_port: ?*c_api.godot_method_bind = null;
var mbind_set_input_data_port_name: ?*c_api.godot_method_bind = null;
var mbind_set_input_data_port_type: ?*c_api.godot_method_bind = null;
var mbind_set_output_data_port_name: ?*c_api.godot_method_bind = null;
var mbind_set_output_data_port_type: ?*c_api.godot_method_bind = null;
var mbind_visual_script_lists_constructor: ?fn () ?*c_api.godot_object = null;

pub const VisualScriptLists = struct {
    const Self = @This();
    const BaseClass = VisualScriptNode;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_visual_script_lists_constructor == null) {
            mbind_visual_script_lists_constructor = try api.createConstructor("VisualScriptLists");
        }
        return api.createObject(Self, mbind_visual_script_lists_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn addInputDataPort(self: *const Self, arg_type: i32, arg_name: *const godot.String, arg_index: i32) !void {
        if (mbind_add_input_data_port == null) {
            mbind_add_input_data_port = try api.createMethod("VisualScriptLists", "add_input_data_port");
        }

        var result: ?*anyopaque = null;
        var args: [3]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_type),
            @ptrCast(*const anyopaque, arg_name),
            @ptrCast(*const anyopaque, *arg_index),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_add_input_data_port, base, cargs, result);
    }

    pub fn addOutputDataPort(self: *const Self, arg_type: i32, arg_name: *const godot.String, arg_index: i32) !void {
        if (mbind_add_output_data_port == null) {
            mbind_add_output_data_port = try api.createMethod("VisualScriptLists", "add_output_data_port");
        }

        var result: ?*anyopaque = null;
        var args: [3]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_type),
            @ptrCast(*const anyopaque, arg_name),
            @ptrCast(*const anyopaque, *arg_index),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_add_output_data_port, base, cargs, result);
    }

    pub fn removeInputDataPort(self: *const Self, arg_index: i32) !void {
        if (mbind_remove_input_data_port == null) {
            mbind_remove_input_data_port = try api.createMethod("VisualScriptLists", "remove_input_data_port");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_index),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_remove_input_data_port, base, cargs, result);
    }

    pub fn removeOutputDataPort(self: *const Self, arg_index: i32) !void {
        if (mbind_remove_output_data_port == null) {
            mbind_remove_output_data_port = try api.createMethod("VisualScriptLists", "remove_output_data_port");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_index),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_remove_output_data_port, base, cargs, result);
    }

    pub fn setInputDataPortName(self: *const Self, arg_index: i32, arg_name: *const godot.String) !void {
        if (mbind_set_input_data_port_name == null) {
            mbind_set_input_data_port_name = try api.createMethod("VisualScriptLists", "set_input_data_port_name");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_index),
            @ptrCast(*const anyopaque, arg_name),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_input_data_port_name, base, cargs, result);
    }

    pub fn setInputDataPortType(self: *const Self, arg_index: i32, arg_type: i32) !void {
        if (mbind_set_input_data_port_type == null) {
            mbind_set_input_data_port_type = try api.createMethod("VisualScriptLists", "set_input_data_port_type");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_index),
            @ptrCast(*const anyopaque, *arg_type),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_input_data_port_type, base, cargs, result);
    }

    pub fn setOutputDataPortName(self: *const Self, arg_index: i32, arg_name: *const godot.String) !void {
        if (mbind_set_output_data_port_name == null) {
            mbind_set_output_data_port_name = try api.createMethod("VisualScriptLists", "set_output_data_port_name");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_index),
            @ptrCast(*const anyopaque, arg_name),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_output_data_port_name, base, cargs, result);
    }

    pub fn setOutputDataPortType(self: *const Self, arg_index: i32, arg_type: i32) !void {
        if (mbind_set_output_data_port_type == null) {
            mbind_set_output_data_port_type = try api.createMethod("VisualScriptLists", "set_output_data_port_type");
        }

        var result: ?*anyopaque = null;
        var args: [2]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_index),
            @ptrCast(*const anyopaque, *arg_type),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_output_data_port_type, base, cargs, result);
    }
};
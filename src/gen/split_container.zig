// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// method bindings
var mbind_clamp_split_offset: ?*c_api.godot_method_bind = null;
var mbind_get_dragger_visibility: ?*c_api.godot_method_bind = null;
var mbind_get_split_offset: ?*c_api.godot_method_bind = null;
var mbind_is_collapsed: ?*c_api.godot_method_bind = null;
var mbind_set_collapsed: ?*c_api.godot_method_bind = null;
var mbind_set_dragger_visibility: ?*c_api.godot_method_bind = null;
var mbind_set_split_offset: ?*c_api.godot_method_bind = null;
var mbind_split_container_constructor: ?fn () ?*c_api.godot_object = null;

pub const SplitContainer = struct {
    const Self = @This();
    pub const BaseClass = godot.Container;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_split_container_constructor == null) {
            mbind_split_container_constructor = try api.createConstructor("SplitContainer");
        }
        return api.createObject(Self, mbind_split_container_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn clampSplitOffset(self: *const Self) !void {
        if (mbind_clamp_split_offset == null) {
            mbind_clamp_split_offset = try api.createMethod("SplitContainer", "clamp_split_offset");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_clamp_split_offset, base, cargs, result);
    }

    pub fn getDraggerVisibility(self: *const Self) !i32 {
        if (mbind_get_dragger_visibility == null) {
            mbind_get_dragger_visibility = try api.createMethod("SplitContainer", "get_dragger_visibility");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_dragger_visibility, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn getSplitOffset(self: *const Self) !i32 {
        if (mbind_get_split_offset == null) {
            mbind_get_split_offset = try api.createMethod("SplitContainer", "get_split_offset");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_split_offset, base, cargs, result);
        return @ptrCast(*i32, @alignCast(@alignOf(&i32), result)).*;
    }

    pub fn isCollapsed(self: *const Self) !bool {
        if (mbind_is_collapsed == null) {
            mbind_is_collapsed = try api.createMethod("SplitContainer", "is_collapsed");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_is_collapsed, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn setCollapsed(self: *const Self, arg_collapsed: bool) !void {
        if (mbind_set_collapsed == null) {
            mbind_set_collapsed = try api.createMethod("SplitContainer", "set_collapsed");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_collapsed),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_collapsed, base, cargs, result);
    }

    pub fn setDraggerVisibility(self: *const Self, arg_mode: i32) !void {
        if (mbind_set_dragger_visibility == null) {
            mbind_set_dragger_visibility = try api.createMethod("SplitContainer", "set_dragger_visibility");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_mode),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_dragger_visibility, base, cargs, result);
    }

    pub fn setSplitOffset(self: *const Self, arg_offset: i32) !void {
        if (mbind_set_split_offset == null) {
            mbind_set_split_offset = try api.createMethod("SplitContainer", "set_split_offset");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_offset),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_split_offset, base, cargs, result);
    }
};
// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const AudioEffect = @import("audio_effect.zig").AudioEffect;

// method bindings
var mbind_get_pan: ?*c_api.godot_method_bind = null;
var mbind_set_pan: ?*c_api.godot_method_bind = null;
var mbind_audio_effect_panner_constructor: ?fn () ?*c_api.godot_object = null;

pub const AudioEffectPanner = struct {
    const Self = @This();
    const BaseClass = AudioEffect;

    // Pointers to zero-sized types are also zero-sized,
    // therefore we need to add some data to make it not zero-sized :)
    _data: u8,
    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_audio_effect_panner_constructor == null) {
            mbind_audio_effect_panner_constructor = try api.createConstructor("AudioEffectPanner");
        }
        return api.createObject(Self, mbind_audio_effect_panner_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getPan(self: *const Self) !f32 {
        if (mbind_get_pan == null) {
            mbind_get_pan = try api.createMethod("AudioEffectPanner", "get_pan");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_pan, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn setPan(self: *const Self, arg_cpanume: f32) !void {
        if (mbind_set_pan == null) {
            mbind_set_pan = try api.createMethod("AudioEffectPanner", "set_pan");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_cpanume),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_pan, base, cargs, result);
    }
};
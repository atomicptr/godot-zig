// This file was automatically generated by godot-zig, please do not edit it.
const godot = @import("__import.zig");
const api = @import("../api.zig");
const c_api = @import("../c_api.zig");

// base class
const Control = @import("control.zig").Control;

// method bindings
var mbind_get_border_color: ?*c_api.godot_method_bind = null;
var mbind_get_border_width: ?*c_api.godot_method_bind = null;
var mbind_get_editor_only: ?*c_api.godot_method_bind = null;
var mbind_set_border_color: ?*c_api.godot_method_bind = null;
var mbind_set_border_width: ?*c_api.godot_method_bind = null;
var mbind_set_editor_only: ?*c_api.godot_method_bind = null;
var mbind_reference_rect_constructor: ?fn () ?*c_api.godot_object = null;

pub const ReferenceRect = struct {
    const Self = @This();
    const BaseClass = Control;

    base: *BaseClass,

    pub fn init() !*Self {
        if (mbind_reference_rect_constructor == null) {
            mbind_reference_rect_constructor = try api.createConstructor("ReferenceRect");
        }
        return api.createObject(Self, mbind_reference_rect_constructor.?);
    }

    pub fn deinit(self: *Self) void {
        _ = api.core.?.godot_object_destroy.?(@ptrCast(*c_api.godot_object, self));
    }

    pub fn getBorderColor(self: *const Self) !godot.Color {
        if (mbind_get_border_color == null) {
            mbind_get_border_color = try api.createMethod("ReferenceRect", "get_border_color");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_border_color, base, cargs, result);
        return @ptrCast(*godot.Color, @alignCast(@alignOf(&godot.Color), result)).*;
    }

    pub fn getBorderWidth(self: *const Self) !f32 {
        if (mbind_get_border_width == null) {
            mbind_get_border_width = try api.createMethod("ReferenceRect", "get_border_width");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_border_width, base, cargs, result);
        return @ptrCast(*f32, @alignCast(@alignOf(&f32), result)).*;
    }

    pub fn getEditorOnly(self: *const Self) !bool {
        if (mbind_get_editor_only == null) {
            mbind_get_editor_only = try api.createMethod("ReferenceRect", "get_editor_only");
        }

        var result: ?*anyopaque = null;
        var cargs: ?*?*const anyopaque = null;

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_get_editor_only, base, cargs, result);
        return @ptrCast(*bool, @alignCast(@alignOf(&bool), result)).*;
    }

    pub fn setBorderColor(self: *const Self, arg_color: *const godot.Color) !void {
        if (mbind_set_border_color == null) {
            mbind_set_border_color = try api.createMethod("ReferenceRect", "set_border_color");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, arg_color),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_border_color, base, cargs, result);
    }

    pub fn setBorderWidth(self: *const Self, arg_width: f32) !void {
        if (mbind_set_border_width == null) {
            mbind_set_border_width = try api.createMethod("ReferenceRect", "set_border_width");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_width),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_border_width, base, cargs, result);
    }

    pub fn setEditorOnly(self: *const Self, arg_enabled: bool) !void {
        if (mbind_set_editor_only == null) {
            mbind_set_editor_only = try api.createMethod("ReferenceRect", "set_editor_only");
        }

        var result: ?*anyopaque = null;
        var args: [1]?*const anyopaque = []?*const anyopaque {
            @ptrCast(*const anyopaque, *arg_enabled),
        };

        var cargs: ?*?*const anyopaque = &args[0];

        const base = @ptrCast(*c_api.godot_object, @alignCast(@alignOf(*c_api.godot_object), self.base));
        _ = api.core.?.godot_method_bind_ptrcall.?(mbind_set_editor_only, base, cargs, result);
    }
};